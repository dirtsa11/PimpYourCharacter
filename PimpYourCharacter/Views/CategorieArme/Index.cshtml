@model IEnumerable<PimpYourCharacter.Models.categorie_arme>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des catégories d'armes</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Label
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.categorie_arme1)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.id_categorie_arme }) |
            @Html.ActionLink("Détails", "Details", new { id=item.id_categorie_arme }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_categorie_arme })
        </td>
    </tr>
}

</table>
