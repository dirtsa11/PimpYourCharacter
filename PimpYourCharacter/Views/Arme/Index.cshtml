@model IEnumerable<PimpYourCharacter.Models.arme>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.label)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.poids)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.categorie_arme.categorie_arme1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.poids)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categorie_arme.categorie_arme1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_arme }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_arme }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_arme })
        </td>
    </tr>
}

</table>
