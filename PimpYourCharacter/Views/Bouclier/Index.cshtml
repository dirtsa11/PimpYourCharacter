@model IEnumerable<PimpYourCharacter.Models.bouclier>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des boucliers</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Label
        </th>
        <th>
            Poids
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.poids)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.id_bouclier }) |
            @Html.ActionLink("Détails", "Details", new { id=item.id_bouclier }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_bouclier })
        </td>
    </tr>
}

</table>
