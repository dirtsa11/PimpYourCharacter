@model IEnumerable<PimpYourCharacter.Models.accessoire>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.label)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.categorie_accessoire.label)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categorie_accessoire.label)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_accessoire }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_accessoire }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_accessoire })
        </td>
    </tr>
}

</table>
