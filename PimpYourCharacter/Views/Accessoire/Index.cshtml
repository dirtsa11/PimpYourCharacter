@model IEnumerable<PimpYourCharacter.Models.accessoire>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des accessoires</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Label
        </th>
        <th>
            Catégorie d'accessoires
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.categorie_accessoire.label)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.id_accessoire }) |
            @Html.ActionLink("Détails", "Details", new { id=item.id_accessoire }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_accessoire })
        </td>
    </tr>
}

</table>
