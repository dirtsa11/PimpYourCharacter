@model IEnumerable<PimpYourCharacter.Models.bras>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des bras</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Longueur
        </th>
        <th>
            Forme
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.longueur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.forme)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.id_bras }) |
            @Html.ActionLink("Détails", "Details", new { id=item.id_bras }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_bras })
        </td>
    </tr>
}

</table>
