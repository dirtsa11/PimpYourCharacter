@model IEnumerable<PimpYourCharacter.Models.bras>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.longueur)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.forme)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.longueur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.forme)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_bras }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_bras }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_bras })
        </td>
    </tr>
}

</table>
