@model IEnumerable<PimpYourCharacter.Models.vmain>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des vêtements main</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Label
        </th>
        <th>
            Poids
        </th>
        <th>
            Couleur
        </th>
        <th>
            Texture
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.poids)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.couleur.label)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.texture.label)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.id_vmain }) |
            @Html.ActionLink("Détails", "Details", new { id=item.id_vmain }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_vmain })
        </td>
    </tr>
}

</table>
