@model IEnumerable<PimpYourCharacter.Models.genre>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des genres</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Label
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.label)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.id_genre }) |
            @Html.ActionLink("Détails", "Details", new { id=item.id_genre }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_genre })
        </td>
    </tr>
}

</table>
