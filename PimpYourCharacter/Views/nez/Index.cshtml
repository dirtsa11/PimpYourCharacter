@model IEnumerable<PimpYourCharacter.Models.nez>

@{
    ViewBag.Title = "Index";
}

<h2>Liste des nez</h2>

<p>
    @Html.ActionLink("Nouveau", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Hauteur
        </th>
        <th>
            Largeur
        </th>
        <th>
            Profondeur
        </th>
        <th>
            Forme
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.hauteur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.largeur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.profondeur)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.forme)
        </td>
        <td>
            @Html.ActionLink("Modifier", "Edit", new { id=item.id_nez }) |
            @Html.ActionLink("Détails", "Details", new { id=item.id_nez }) |
            @Html.ActionLink("Supprimer", "Delete", new { id=item.id_nez })
        </td>
    </tr>
}

</table>
